<%= form_with(model: claim, local: true) do |form| %>
  <% if claim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(claim.errors.count, "error") %> prohibited this claim from being saved:</h2>

      <ul>
      <% claim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :claim_number %>
    <%= form.text_field :claim_number %>
  </div>

  <div class="field">
    <%= form.label :initiation_date %>
    <%= form.date_select :initiation_date %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :approved_amount %>
    <%= form.text_field :approved_amount %>
  </div>

  <div class="field">
    <%= form.label :claim_reason %>
    <%= form.text_area :claim_reason %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
